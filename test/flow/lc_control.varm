using lenof, prompt, putln:
{
  let s := loop[] {
    let x := prompt("Don't just type 'Enter' > ")
    if lenof(x) = 0 then
      continue "D'oh"
    else
      break "You did it champ"
  }
  s
}
# ["D'oh", "D'oh", "D'oh", ..., "You did it champ"]
