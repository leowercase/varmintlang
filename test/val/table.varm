let tb := {
  .key := "value",
  ["nope"] := { [True] := False },
  [-0.09] := "negative zero point oh niner",
}

using putln: {
  putln(tb[-0.09])

  if not tb.nope[True] then
    putln("yep, nope.")
}
